// 8 kyu kata

package kata

import "strings"

func StringToArray(str string) []string { return strings.Split(str, " ") }

// a very easy one, but helpful because it made me look into the strings library
